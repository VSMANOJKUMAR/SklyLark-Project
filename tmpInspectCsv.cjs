const fs = require('fs'); const Papa = require('papaparse'); const txt = fs.readFileSync('Deal funnel Data.xlsx - Deal tracker.csv','utf8'); const parsed = Papa.parse(txt,{header:true,skipEmptyLines:true}); console.log('headers', parsed.meta.fields); console.log('first row keys', Object.keys(parsed.data[0])); console.log('first row sector/service', parsed.data[0]['Sector/service']); console.log('second row sector/service', parsed.data[1]['Sector/service']); console.log('row count', parsed.data.length);
